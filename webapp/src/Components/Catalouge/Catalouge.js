import React, { useState } from 'react'

function Catalouge(props) {
    return (

        <div className={`${props.viewCatalog ? "card1" : "catalogue-card"}`}>
            i am moving
        </div>
    )
}

export default Catalouge
